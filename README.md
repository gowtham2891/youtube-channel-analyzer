# YouTube Channel Analyzer

[![Live demo](https://static.streamlit.io/badges/streamlit_badge_black_white.svg)](https://youtube-channel-analyzerr.streamlit.app)
[![CI](https://github.com/gowtham2891/youtube-channel-analyzer/actions/workflows/ci.yml/badge.svg)](https://github.com/gowtham2891/youtube-channel-analyzer/actions/workflows/ci.yml)
[![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

**▶ Try it live — [youtube-channel-analyzerr.streamlit.app](https://youtube-channel-analyzerr.streamlit.app)** · runs in mock mode, no credentials needed — or paste your own API keys in the sidebar to drive the live providers.

Point it at a YouTube channel. It fetches the uploads and transcripts, computes
what actually happened, and uses **Gemini only for the qualitative read** —
themes, hook patterns, gaps, and what to make next.

The split is deliberate: **every number in the report is computed in Python, not
generated by a model.** LLMs are unreliable at arithmetic and very convincing
when wrong, so they never touch the statistics.

---

## Pipeline

```
  @channel  ·  youtube.com/…  ·  UC…
        │
        ▼
┌──────────────────┐  Resolves handles, URLs, legacy /user/ and /c/ paths,
│  Resolve channel │  falling back to search when forHandle misses
└────────┬─────────┘
         ▼
┌──────────────────┐  Walks the uploads playlist, batches videos.list 50 at
│  Fetch uploads   │  a time (the API's hard cap)
└────────┬─────────┘
         ▼
┌──────────────────┐  Median · engagement · cadence · consistency · outliers
│  Compute stats   │  · weekday performance · duration buckets · tag frequency
└────────┬─────────┘                        ← pure Python, fully tested
         ▼
┌──────────────────┐  Reads the most-viewed videos' transcripts; missing
│  Read transcripts│  captions degrade, never abort
└────────┬─────────┘
         ▼
┌──────────────────┐  Titles + tags + transcript hooks → themes, patterns,
│  Gemini analysis │  opportunities, recommended topics
└──────────────────┘
```

---

## Quickstart

```bash
git clone https://github.com/gowtham2891/youtube-channel-analyzer.git
cd youtube-channel-analyzer
pip install -e ".[dev,ui]"

# Full analysis of a synthetic channel — no API keys
yt-analyzer demo
```

```
             Performance
┌────────────────────┬──────────────┐
│ Videos analyzed    │ 50           │
│ Median views       │ 22,784       │
│ Mean engagement    │ 4.29%        │
│ Shorts / long-form │ 8 (16%) / 42 │
│ Upload cadence     │ 4.2/month    │
│ Consistency        │ 59/100       │
│ Best upload day    │ Saturday     │
└────────────────────┴──────────────┘

Outliers (more than 2x the median)
  • 211,505 — Stop Using Fine-Tuning Wrong
  • 193,563 — I Built FastAPI Backends in 3 Hours
```

### Web UI

```bash
streamlit run app.py
```

Five tabs: channel overview with stat tiles, upload/performance trends, a
sortable video table with links, the LLM's content insights, and markdown/JSON
export.

### CLI

```bash
# Live analysis
yt-analyzer analyze @mkbhd --source youtube --analyzer gemini \
    --max-videos 100 --output report.md

# Any identifier form works
yt-analyzer analyze "https://www.youtube.com/channel/UCBJycsmduvYEL83R_U4JriQ"
yt-analyzer analyze UCBJycsmduvYEL83R_U4JriQ

# Skip transcripts — much faster, metadata only
yt-analyzer analyze @veritasium --source youtube --no-transcripts

# Verify your API keys actually work before spending quota
yt-analyzer check
```

---

## What it computes

| Metric | Why it's there |
| --- | --- |
| **Median** views (not just mean) | One viral video makes a mean useless. The median is what a typical upload does. |
| **Outliers** (>2× median) | Measured against the median, so a viral hit can't hide the *other* overperformers. |
| **Consistency score** (0–100) | Coefficient of variation on upload gaps. 100 = perfectly even spacing. |
| **Upload cadence** | Counts *intervals*, not videos — n uploads span n−1 gaps, so dividing by video count overstates the rate by n/(n−1). |
| **Weekday performance** | Ranked by **mean views per day**, not upload count. Posting most often on Monday says nothing about whether Monday works. |
| **Duration buckets** | Mean views per length band — does long-form actually outperform Shorts on *this* channel? |
| **Engagement rate** | (likes + comments) / views, per video and averaged. |

---

## Getting API keys

**YouTube Data API v3** — free, 10,000 quota units/day:
1. Create a project at [console.cloud.google.com](https://console.cloud.google.com)
2. Enable **YouTube Data API v3**
3. Create an API key → `YOUTUBE_API_KEY`

A 50-video analysis costs roughly 5 units, so the free tier is generous.

**Gemini** — free tier at [aistudio.google.com](https://aistudio.google.com) → `GEMINI_API_KEY`

**Transcripts** need no key: `pip install -e ".[transcripts]"`

---

## Mock mode

`DATA_SOURCE=mock` generates a deterministic synthetic channel of 50 videos with
uneven view counts, two planted outliers, mixed Shorts and long-form, irregular
upload gaps, and **transcripts missing for about a third of videos** — exactly
how the real API behaves.

That realism is the point. Statistics tests run against data with genuine
variance and genuine gaps, so they catch things a uniform fixture never would
(the cadence estimator bug above was found this way).

---

## Configuration

| Variable | Purpose | Default |
| --- | --- | --- |
| `DATA_SOURCE` | `mock` or `youtube` | `mock` |
| `ANALYZER` | `mock` or `gemini` | `mock` |
| `YOUTUBE_API_KEY` | Data API v3 key | — |
| `GEMINI_API_KEY` / `GEMINI_MODEL` | Gemini credentials | `gemini-2.0-flash` |
| `MAX_VIDEOS` | Uploads to fetch | `50` |
| `MAX_TRANSCRIPTS` | Transcripts to read (most-viewed first) | `10` |
| `TRANSCRIPT_LANGUAGES` | Preference order | `en,te,hi` |
| `FETCH_TRANSCRIPTS` | Read transcripts at all | `true` |
| `MAX_RETRIES` | Retries on transient API errors | `3` |

---

## Design notes

**Numbers and narrative are separated.** `analytics.py` is pure Python with no
provider dependency, and it runs *before* the LLM stage. If Gemini fails, times
out, or isn't configured, the pipeline logs it and returns the full quantitative
report anyway — tested explicitly.

**Failure is expected, not exceptional.** Missing captions are the norm
(disabled, music, age-restricted), so `fetch_transcript` returns a `Transcript`
with `error` set rather than raising. A 403 from the Data API is translated into
a message naming the three actual causes: bad key, API not enabled, quota gone.

**Hidden statistics are handled.** Uploaders can hide like counts; the API then
omits the field entirely rather than sending zero. Every parse defaults safely.

**Charts follow one rule set.** Single-series, single-hue, no dual-axis anywhere
— views and upload counts get separate charts rather than a shared distorted
scale. Every chart ships with a table view beside it.

---

## Project structure

```
yt_analyzer/
├── models.py       # Channel, Video, Transcript, report rendering
├── analytics.py    # All statistics — pure Python, no providers
├── pipeline.py     # Orchestration, stats-before-LLM ordering
├── cli.py          # analyze · providers · demo
└── providers/
    ├── base.py     # Two ABCs, two registries
    ├── youtube.py  # Data API v3 + identifier parsing + transcripts
    ├── gemini.py   # Qualitative analysis with schema validation
    └── mock.py     # Synthetic channel + heuristic analyzer
app.py              # Streamlit UI
tests/              # 115 tests, all offline
```

---

## Testing

```bash
pytest -v
pytest --cov=yt_analyzer --cov-report=term-missing
```

Covers ISO-8601 duration parsing, every channel-identifier form, API response
shapes including hidden statistics, median-based outlier detection, cadence and
consistency estimators, weekday ranking by performance rather than volume,
transcript-failure isolation, 403/quota handling, retry-then-fail behaviour, and
an explicit test that **a broken analyzer still returns complete statistics**.

---

## Roadmap

- [ ] Compare two channels side by side
- [ ] Thumbnail analysis with a vision model
- [ ] Comment sentiment sampling
- [ ] Track a channel over time and diff the reports

---

## Bring your own API keys

The demo runs in mock mode, but you don't have to take its word for the live
path. Open **Use your own API keys** in the sidebar, paste a YouTube Data API or Gemini key,
and press **Test connections**. Each provider is probed with the smallest
authenticated request it supports, and you get a specific verdict rather than a
generic failure:

| Verdict | What it means |
| --- | --- |
| Key works — 68 models available | Authenticated and ready |
| Key rejected: API key not valid | Wrong, revoked, or mistyped key |
| Rate limited — valid but throttled | The key is fine; the quota isn't |
| Quota exhausted / API not enabled | Actionable: says exactly what to fix |

Keys entered this way stay in your browser session. They are **never** written
to `os.environ` — a deployed Streamlit app serves every visitor from a single
process, so a key placed in the environment would leak into other visitors'
sessions. That property is pinned by a test that drives the real app and fails
the build if a key ever reaches the process environment.

### If a key is missing

Pick a live provider without its key and the app says so before it runs
anything, naming the variable, the provider that needs it, and where to get
one — then opens the key panel for you. The run is blocked rather than
failing part way through:

```
Add an API key to continue.
- YouTube Data API key - needed for the 'youtube' data source
  get one at console.cloud.google.com

Open "Use your own API keys" in the sidebar and paste it there,
or switch the provider back to `mock` to use the offline demo.
```

The CLI does the same and exits `1`:

```bash
$ yt-analyzer check
```

From the terminal:

```bash
yt-analyzer check
```

Exit code `0` if every configured credential works, `2` if any fails.

---

## Deploy your own

Ready for [Streamlit Community Cloud](https://share.streamlit.io): free, and it
redeploys on every push to `main`.

1. Sign in at [share.streamlit.io](https://share.streamlit.io) with GitHub.
2. **Create app** → this repo, branch `main`, main file `app.py`.
3. Under **Advanced settings**, choose Python **3.11**.
4. Set the custom subdomain to `youtube-channel-analyzerr` (this app's URL).
5. Deploy — the first build takes a couple of minutes.

No secrets are needed for the demo. To switch it to the live providers, open
**Settings → Secrets** in the Streamlit dashboard and paste:

```toml
DATA_SOURCE = "youtube"
ANALYZER = "gemini"
YOUTUBE_API_KEY = "your-youtube-key"
GEMINI_API_KEY = "your-gemini-key"
```

`app.py` copies those secrets into the environment before settings are resolved,
so the exact same configuration works locally through `.env` and in the cloud
through the dashboard — no code changes either way.

---

## License

MIT — see [LICENSE](LICENSE).

**Ganesh Gowtham Dupati** · [GitHub](https://github.com/gowtham2891) · gowthamdupati28@gmail.com
